REACT_APP_DEBUG=DEBUG
# The dev server proxies the backend (see src/setupProxy.js), so the app talks to
# whatever host it is served from -- the same code path production uses.
REACT_APP_SAME_ORIGIN=true
